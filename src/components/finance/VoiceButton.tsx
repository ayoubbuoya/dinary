import { Mic, Square } from 'lucide-react-native';
import { Pressable, StyleSheet, View } from 'react-native';
import { colors, radius } from '@/constants/colors';
import { Text } from '@/components/ui/Text';
export function VoiceButton({ onPress, compact = false, isListening = false }: { onPress?: () => void; compact?: boolean; isListening?: boolean }) { return <Pressable accessibilityRole="button" accessibilityLabel={isListening ? 'Stop voice entry' : 'Start voice entry'} accessibilityHint="Your speech is transcribed into a draft for review before it can be saved." onPress={onPress} style={({ pressed }) => [styles.button, isListening && styles.listening, compact && styles.compact, pressed && styles.pressed]}><View style={styles.icon}>{isListening ? <Square color={colors.expense} fill={colors.expense} size={16} /> : <Mic color={colors.primary} size={20} />}</View>{!compact && <View><Text style={styles.title}>{isListening ? 'Listening…' : 'Voice entry'}</Text><Text variant="caption">{isListening ? 'Tap to finish' : 'Create a draft to review'}</Text></View>}</Pressable>; }
const styles = StyleSheet.create({ button: { minHeight: 56, borderRadius: radius.md, backgroundColor: colors.primarySoft, padding: 10, flexDirection: 'row', alignItems: 'center', gap: 10 }, listening: { backgroundColor: colors.accentSoft }, compact: { width: 56, justifyContent: 'center' }, icon: { width: 36, height: 36, borderRadius: 18, justifyContent: 'center', alignItems: 'center', backgroundColor: colors.white }, title: { fontWeight: '700', color: colors.primary }, pressed: { opacity: .75 } });
